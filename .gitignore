# Environment & Secrets — NEVER commit these
.env
.env.local
.env.staging
.env.production

# Dependencies
node_modules/
npm-debug.log*

# File uploads (handled by cloud storage in production)
src/storage/uploads/

# OS artifacts
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Local agent + design artifacts (never deploy)
.claude/
SFProExtracted copy/
.env.local.bak*

# Raw source images (optimize into assets/images before use)
New Images/
new images3/
New Images3/
image-catalog.html
images copy/

# Consolidated local image archive — every image folder NOT served by the app
# (raw dumps, source packs, small-image backups) lives here. Kept on disk for
# reference, never committed or deployed. The live folder is assets/images/.
image-library/
